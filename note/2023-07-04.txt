20230704 일곱번째 수업

오늘은 static과 객체와의 관계에 대해 배웠다.
※ static 변수를 클래스 변수(정적변수, 공용변수)라 부릅니다.
객체는 클래스에서 생성(복사)된다고 배웠습니다.
static이붙은 변수는 객체 변수가 아닌 클래서 변수로써 객체가 생성되기 전에 이미 만들어놓고 불러와서 사용합니다.
static은 객체를 생성하지 않고도 사용이 가능하지만 메모리를 많이 차지하기 때문에 필요할때만 사용해야합니다. 